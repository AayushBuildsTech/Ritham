# @daily-co/config-plugin-rn-daily-js declares an outdated peer range, so a
# clean `npm install` (as EAS Build runs) fails with ERESOLVE. Allow npm to
# proceed past that peer conflict — matches how the lockfile was generated.
legacy-peer-deps=true
